#include <stdio.h>
#include <stdlib.h>

/* This program should print numbers from 0 - 20 */

int main()
{
        int a;

        for (a=0; a<=20; a++)
                printf("\t%d", a);
        return (0);
}
